# 2026-06-05 工作日志

## ArXiv 论文日报自动追踪
- 自动化任务执行成功
- 从 ArXiv RSS Feed 获取 1013 篇论文，关键词过滤后 192 篇
- 分组：A组(检测强相关) 81篇 / B组(端云协同) 24篇 / C组(其他) 87篇
- 标记值得精读 57 篇
- 生成日报 HTML 并推送至 GitHub Pages: https://miclover0.github.io/paper-daily/
- Git commit: e61ccd6

## 自动化任务 — 22:55 再次执行
- 论文数据与首次一致（同日运行）：192 篇，A组81 / B组24 / C组87，精读57篇
- Git commit 成功 (3189a31): 包含 config.js、HTML 及辅助文件共计 17 个文件变更
- Git push 失败: GitHub 不可达（connection refused on port 443），GITHUB_TOKEN 未设置
- 需在网络恢复后手动 `git push origin main`

## 论文筛选优化 — 23:34
- **需求**：每天近200篇论文太多，需要筛选强相关50篇 + 精读推荐20篇(含精读原因)
- **实现方案**：
  1. 新增 `compute_relevance_score()` 函数，基于类别/关键词/负面词计算 0-100 相关度分数
  2. 新增 `select_top_papers()` — 按相关度分数筛选前50篇强相关
  3. 新增 `generate_read_reason()` — 基于规则模板生成中文精读原因
  4. 新增 `select_featured_papers()` — 综合相关度(50%)+质量评分(50%)挑选20篇精读推荐
- **修改主流程** `main()`: Step 2.5 筛选强相关 → Step 2.6 精读推荐 → 用筛选后数据生成 HTML/更新 config
- **修改** `update_config_json()`: 支持 `featured_papers` 参数，在 `featuredPapers` 中添加 `readReason` 字段
- **修改** `generate_daily_html()`: 添加 `.read-reason-box` CSS 样式和精读原因 div
- **运行结果**: 192篇 → 50篇强相关(分数>=72) → 20篇精读推荐(分组: A=25/B=5/C=20)
- Git commit: cfe785a，已推送至 GitHub Pages

## 二级页面三大功能增强 — 23:53
- **需求1: 锚点跳转** — index.html 点击论文跳转到二级页面对应位置
  - `generate_daily_html()`: 每个 card 添加 `id="A1"` 等锚点
  - `update_config_json()`: 给每篇 paper 添加 `anchorId` 字段
  - `index.html`: "查看完整日报 HTML" 按钮链接带上 `#A1` 锚点
- **需求2: 左右两栏布局** — 左侧目录 + 右侧内容
  - 完全重写 `generate_daily_html()`: `<aside class="toc">`(280px固定) + `<main class="content">`(flex:1)
  - 左侧 TOC 列出每篇论文标题+锚点链接，精读推荐加 ⭐ 标记
  - 响应式设计：手机端变为上下布局
- **需求3: 完整中文摘要** — 不再是单句话
  - 重写 `generate_chinese_summary()`: 新增 `truncate_en_sentence()` 辅助函数
  - 产出结构化摘要：【研究背景】【核心方法】【实验结果】【主要贡献】（4-5段）
  - 摘要长度从 ~100 字符增加到 ~400 字符
- **运行结果**: 三项功能全部验证通过，本地预览正常
- Git commit: 9a2753a（push 失败，网络不可达，待恢复后执行 `git push origin main`）
